# Gate F disposable test project

This fixture is intentionally safe to copy and modify during real-machine tests.
It contains one Markdown brief, two image references, one JSON note file and one YAML skill-like resource.
It contains no `.creative-os` runtime data, database, token, log or provider session.
