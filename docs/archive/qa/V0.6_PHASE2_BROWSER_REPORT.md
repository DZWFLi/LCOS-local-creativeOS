# Local Creative OS v0.6 Phase 2 轻量浏览器验收

**总结果：FAIL（A–H 已逐项完成或通过 Fixture 绕过前置失败验证）**  
日期：2026-07-22  
保留 URL：`http://127.0.0.1:5191/?state=review`  
环境：真实 Windows Chrome；1440×900 主测；1366 档受宿主整数换算实际为 1367×768；独立 Vite 端口 5191。监听进程已确认来自 Phase 2 新临时解压目录。未修改产品代码，未运行完整 `npm run check`、跨浏览器、压力或缩放矩阵。

## 缺陷摘要

1. **P0｜`Ctrl+Enter` 跳过确认直接执行。** `phase2-single` 输入要求后按 `Ctrl+Enter`，没有出现 `.run-confirm-dialog`，Canvas 未锁定，Process 由基线 2 增至 3，Work Rail 直接进入 `data-mode="run"` 并创建 RUN-044。点击 Composer 的“发送指令”在 Continue Modify 路径能正常打开确认，因此问题集中在快捷键发送路径。
2. **P1｜Continue Modify 未聚焦 Composer。** 点击“继续修改”后，Pending/返回结果被选为目标且文本出现“继续修改：”，但活动元素仍是“继续修改”按钮，Composer 未获得焦点。
3. **P1｜`C` 未聚焦 Composer。** 在 `phase2-multi` 按 C 后活动元素仍为 BODY；Process 数量未变化，未误创建 Run。
4. **P1｜Accept 后新 Run Process 未紧凑化。** Accept 表面成功，但新 Run Process 仍为 `density-standard`；仅旧 `activity` 为 `density-compact`。

应用侧 Console 阻断 error：**0**。

## A–H 独立结果

| 项目 | 结果 | 真实观察 |
|---|---|---|
| A. Phase1.1 锁定回归 | PASS | 遮罩单击、左拖、中键拖、滚轮、Ctrl+滚轮后：dialog 仍在、`data-locked=true`、Camera `translate3d(92px,42px) scale(0.94)` 不变、8 节点位置不变、Canvas DOM=1 |
| B. 单文件黄金路径 | **FAIL / P0** | 初始 Work Rail 单实例、`selection`、proposal 已选中；但 `Ctrl+Enter` 跳过确认，直接新增 RUN-044/Process 并进入 `run`。运行期 Composer 存在且发送禁用、Canvas DOM 未卸载 |
| C. waiting_input 继续 | PASS（前置 B 失败后沿已启动 Fixture 继续） | 自动进入 `waiting-input`；选择 35% 后自动回到 `run`；随后自动推进 review |
| D. Artifact Return / Review | PASS（Fixture） | Work Rail 单实例并进入 `review`；新 Generated Draft 位于 `left:1166px`，原 Target `left:432px`，无重叠；前后预览、摘要、变化文件、接受/继续修改均出现；Canvas DOM=1。Camera 自动带入结果未做数值前后对比 |
| E. Continue Modify | PARTIAL | 返回结果被选为下一 Target，文本以“继续修改：”开头，再点击发送可打开锁定确认且 Target 为返回结果；但 Composer **未自动聚焦** |
| F. Accept Current | PARTIAL（通过 `?state=review` Fixture 绕过） | Work Rail→`selection`，显示“已接受为当前版本”；结果节点由 Generated 表面晋升为 Working/Current，旧 Target 不再显示 Current，Checkpoint 建议出现；但新 RUN-044 Process 仍为 `density-standard`，未紧凑化。未把该段算作原 RUN-044 端到端通过 |
| G. 多选推断 | PASS | `phase2-multi` 为 `multi-selection`；proposal 自动为目标，feedback/reference/brief 为参考。再 Shift 加入第二可编辑文件后只出现“这次主要修改哪个文件？”，选择 V3 后另一文件自动转为参考且发送立即可用 |
| H. 快捷键与 Workspace | PARTIAL | `C` 不创建 Process，但也没有聚焦 Composer（FAIL）；切到“Thinker 创意探索”后选择清空、Work Rail 回到 `workspace`（PASS）；始终只有一个 Work Rail |

## Phase 1 遗留补测

本节使用同一个 Phase 2 正确包的 5191 服务补齐此前因 P0 停止而未执行的 Phase 1 浏览器项。Phase 2 已覆盖的 Target 歧义和创建时机直接映射到 Phase 1 标准，不重复制造截图。

| Phase 1 遗留项 | 结果 | 证据与说明 |
|---|---|---|
| 确认框打开/关闭/拖画布循环 5 次 | PASS | 五轮中 Canvas CDP DOM Node ID 始终为 41、Canvas DOM 始终 1、节点数始终 10（包含此前 Phase 2 Fixture 新增的 2 个节点）。关闭和重新打开确认框时 Camera 均不跳动；关闭后中键拖动画布每轮都能正常改变 Camera，重新打开后保持拖动结果；未观察到全白、全灰或节点消失。循环后 Camera 从 `translate3d(143.692px,200.841px)` 逐轮正常推进至 `translate3d(358.692px,310.841px)`，缩放始终 `0.828784` |
| textarea 连续输入 100 字 | PASS | 重置 Fixture 后恢复 8 节点基线；输入前后 Canvas DOM Node ID 相同、Canvas DOM=1、节点=8、Camera 始终 `translate3d(92px,42px) scale(0.94)`，textarea 实际长度 100；未观察到闪白、闪灰或节点重建 |
| 首屏字段简化与执行方式折叠 | PASS | 默认仅出现修改要求、目标、参考、执行方式、返回修改、开始执行；展开“执行方式：Codex · 保存为新版本”后才出现“执行器 / Codex / 结果处理 / 保存为新版本”，未默认展示 Skill/Executor/Output 大表单 |
| 两个可编辑文件 Target 歧义 | PASS（引用 Phase 2 G） | 只出现“这次主要修改哪个文件？”；选择 V3 后发送按钮立即可用，另一可编辑文件自动进入参考，参考数量由 3 变 4 |
| Process / Run 创建时机 | PASS（按钮路径）/ FAIL（快捷键路径） | 重置后的正常确认面板中，点击“开始执行”前 Process=2 个历史 Fixture 节点；点击后只新增 1 个 Process（2→3），Work Rail=`run`，显示 RUN-044，并提示“参考快照、指令和执行记录已自动保存”。Run ID 可见；Command ID 与 Context Snapshot ID 未暴露在渲染 DOM，无法仅靠本轮轻量浏览器证据核对具体 ID。另见 Phase 2 B：`Ctrl+Enter` 会跳过确认，仍为 P0 |
| 1440×900 居中几何 | PASS（引用既有数据） | 可用 Canvas `(228,52)–(1090,900)`；dialog `(380,170.25) 560×605.77`；中心偏差 `(+1.00,-2.87)px`；无外层滚动 |
| 1366×768 居中与内部滚动 | PASS（宿主取整实测 1367×768） | 可用 Canvas `(228,52)–(1055.47,768)`；dialog `(362.73,67) 560×680`；中心偏差 `(+1.00,-3.00)px`；页面 `scrollWidth/Height=1367/768`。100 字内容并展开执行方式后，`.run-confirm-body` 为 `overflow-y:auto`，`scrollHeight=561 > clientHeight=544`，仅面板 Body 内部滚动 |

## DOM / 几何 / 视口

- 1440×900 `?state=confirm`：可用 Canvas `(228,52)–(1090,900)`；dialog `(380,170.25) 560×605.77`；中心偏差 `x=+1.00px, y=-2.87px`，通过 8px 阈值；页面 `scrollWidth/Height=1440/900`；Work Rail=1、Canvas DOM=1、`data-locked=true`。
- 确认首屏字段通过：你想怎么修改、修改目标、参考内容、折叠的执行方式、返回修改、开始执行；未默认展开 Skill/Executor/Output。
- 1366 档：宿主缩放的整数 CDP 指标只能得到 **1367×768**；页面无外层溢出，Accepted 关键态 Work Rail=1、Canvas DOM=1、`data-mode=selection`。
- 全程应用侧 Console error=0。

## 截图（13 张）

1. `02-P0-all-mask-gestures-pass.png`
2. `03-phase2-single-selection-composer.png`
3. `04-P0-ctrl-enter-skips-confirm-and-starts-run.png`
4. `05-waiting-input-35-choice.png`
5. `06-artifact-return-review.png`
6. `07-continue-modify-draft-target.png`
7. `08-accept-current-and-checkpoint.png`
8. `09-phase2-multi-target-context.png`
9. `10-multi-two-editable-target-question.png`
10. `11-accepted-key-state-1367x768-rounding.png`
11. `12-phase1-five-cycle-stability.png`
12. `13-phase1-100-char-input-stability.png`
13. `14-phase1-confirm-1367x768-geometry.png`

## 范围声明

RUN 的 queued/running/waiting_input/review、返回文件和 Changed Files 均由前端 Fixture 推进，不代表 Local Core、Bridge、Codex Runtime、真实文件写回或持久化已接通。未测试真实后端、跨浏览器、20 次压力、缩放矩阵或完整交付质量链。
