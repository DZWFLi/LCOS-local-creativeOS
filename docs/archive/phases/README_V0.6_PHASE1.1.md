# Local Creative OS v0.6 Phase 1.1

这是 Phase 1 的 P0 Canvas 锁定热修，不是 Phase 2。

修复内容：

- 遮罩拖动不再关闭任务确认；
- 遮罩捕获完整指针手势；
- Canvas 锁定时禁用全部指针交互；
- Canvas 事件处理器增加锁定硬保护；
- 创建内容面板同步使用相同锁定合同；
- 仅允许关闭按钮、返回按钮和 Esc 解除锁定。

浏览器验收见 `CODEX_RUN_V0.6_PHASE1.1.md`。
