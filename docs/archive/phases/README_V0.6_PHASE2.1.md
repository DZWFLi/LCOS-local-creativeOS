# Local Creative OS v0.6 Phase 2.1

本版是 Phase 2 浏览器报告后的主闭环热修，范围严格限定为：

1. `Ctrl/Cmd + Enter` 必须先打开执行确认，不得直接创建 Run；
2. `C` 与“继续修改”必须真实聚焦常驻 Composer；
3. 接受结果后，本次新建的 Process Node 必须立即转为紧凑完成态；
4. Phase 1.1 的 Canvas 锁定合同完整保留。

没有提前混入 Phase 3 的 Project Drive / Child Canvas 新功能。

测试入口与停止规则见 `CODEX_RUN_V0.6_PHASE2.1.md`。
