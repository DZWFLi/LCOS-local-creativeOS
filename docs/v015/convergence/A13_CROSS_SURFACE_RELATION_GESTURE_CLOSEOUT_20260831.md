# LCOS v0.15 · A13 Cross-surface Relation Gesture Adapter Closeout

Date: 2026-08-31
Status: **SOURCE / STATIC PASS · MERGE-AUTHORIZED · PHASE A STILL OPEN**

Product proposition:

> Main / Context / Workflow ordinary eligible Project-view materials share one explicit `Select → Orbit → Relation → Orbit yields → transient source → pointer-follow → receptive target → commit` physical grammar, while each Surface keeps its own canonical persistence semantics.

---

# 0. Final verdict

```text
Authoritative real-RC base:
2d2f30cb0f68ac50d02e498a13b43d5687d95b49

SOP-R1 reconstructed-authority gate:
8/8 PASS

8/21 v0.3 raw source:
RAW_SOURCE_LOST / RECONSTRUCTED_AUTHORITY PASS
A13_BLOCKING = NO

A13 implementation:
SOURCE / STATIC PASS

A13 dedicated validator:
12/12 PASS

Merge authorization:
YES

Post-A13 semantic typecheck in extracted ZIP env:
BLOCKED_ENV (no node_modules; node + vite/client types unavailable)

Unit / Vitest in extracted ZIP env:
BLOCKED_ENV

Browser E2E:
BLOCKED_ENV

Human Product Smoke:
BLOCKED_ENV

Phase A complete:
NO

Phase B admission:
NO
```

A13 is therefore formally closed as a **source/owner micro-patch** and may be merged into the user's real local `2d2f30c` lineage. Runtime/manual evidence must be rerun after local merge and remains debt, not invented PASS.

---

# 1. Provenance / SOP adjudication

The real merged-RC validation established `2d2f30c` as the authoritative A00–A12 baseline. The uploaded ZIP has no `.git`, so lineage truth is accepted from that validation rather than falsely re-proven in the archive.

The previously blocking 8/21 v0.3 raw source has been adjudicated by the new SOP-R1 rule:

```text
RAW_SOURCE_LOST
→ Provenance Ledger
→ surviving upstream/peer originals
→ later explicit Freeze / L0
→ delta audit
→ current code/test evidence
→ latest user adjudication
→ RECONSTRUCTED_AUTHORITY PASS
```

This does **not** claim verbatim recovery. It records that the lost construction master is not the sole product authority and that the current A13 product truth is independently recoverable with high confidence.

Authoritative ledger:

`docs/v015/convergence/LOST_SOURCE_PROVENANCE_LEDGER_20260831.md`

---

# 2. Source-Diff Gate

```text
Original / surviving authority:
- three first-class Main / Context / Workflow work surfaces;
- shared Spatial interaction primitives;
- same Project Truth, different Surface semantics;
- Relation is spatial interaction, not a second form/page.

Later explicit Freeze / L0:
- same physical grammar across three Surfaces;
- Surface semantic truth remains distinct;
- do not flatten Workflow into a generic DAG/connect model;
- latest 2026-08-31 user adjudication authorizes reconstructed authority and formal A13.

Latest reality feedback:
- permanent / hover-only tiny Relation notch is too obscure as primary launch owner;
- Visual Bounds != Interaction Bounds;
- one dominant transient interaction owns the local moment.

Current construction clause:
Phase A · Shared Spatial Kernel / Production Owner Cleanup.

Previous owner state:
- Main ordinary object had Orbit → Relation physical entry;
- Context / Workflow ordinary Project material lacked equivalent shared physical Relation session;
- Context/Workflow canonical persistence truths already existed separately.

Classification:
IMPLEMENTATION_GAP
+ WRONG_OWNER avoidance
```

No surviving authority conflicts with A13.

---

# 3. Canonical owner split after A13

A13 deliberately keeps four truths separate:

```text
1. shared physical Relation gesture
2. Main edge/connect persistence
3. Context domain Relationship/Relation persistence
4. Workflow material Relation vs Step/action path truth
```

Shared physical owner:

`apps/web/src/features/spatial/projectMaterialRelationGesture.tsx`

It owns only transient:

- source id / point;
- live pointer point;
- receptor target id;
- Escape/cancel;
- commit handoff.

It does not own `saveRelation`, Main edge store, Workflow action edge store, bridge state, or relation ontology.

Persistence remains:

```text
Main:
ProjectCanvas existing edge/connect truth

Context:
onCreateDomainRelation(..., 'context-canvas')

Workflow material:
onCreateDomainRelation(..., 'workflow-canvas')

Workflow Step/action:
workflowActionState.edges + workflow action ports
```

No universal semantic `connect()` was introduced.

---

# 4. Endpoint admission / fail-close

A13 exposes Project-material Relation only where the current canonical callback can prove view→view semantics.

Eligible:

```text
ordinary Project View material
AND not Conversation Glyth
AND not scope:* aggregate projection
AND not workspace:* aggregate projection
```

Still fail-closed:

- Conversation Glyth Relation endpoint semantics;
- aggregate scope projection endpoints;
- aggregate workspace projection endpoints.

This is intentional semantic safety, not missing polish.

---

# 5. Physical interaction

For eligible Context / Workflow Project materials:

```text
Click
→ Selection
→ ProjectObjectOrbit
→ Relation
→ Orbit yields
→ shared transient relation session
→ passive source port
→ pointer-follow live edge
→ explicit Project-material receptor
→ target commit
→ transient cleanup
→ Surface canonical persistence callback
```

During intent:

- Semantic Drop handle yields;
- Workflow action ports / bypass controls yield visually;
- target pointer intent is consumed before Selection/Semantic Drop can compete;
- follow-up click does not immediately replace the completed Relation action with a new target Orbit.

---

# 6. Main target safety correction

Main target discovery no longer treats every generic `[data-node-id]` as an ordinary Relation receptor.

A13 uses explicit:

`[data-project-relation-target]`

Workspace legacy `[data-relation-target]` remains isolated debt.

Conversation CanvasCard does not receive ordinary Project-material Relation receptor semantics.

---

# 7. Donor conformance

Relevant donor principles:

- Lovart / TapNow object-local satellites and causal continuity;
- Codex Desktop local anchor vs persistent panel separation;
- TodoPanel one-dominant-transient swap grammar.

Borrowed craft:

```text
current object
→ local Orbit action
→ Orbit yields
→ small source affordance
→ direct pointer-follow
→ local target receptivity
```

Not copied:

- donor taxonomy;
- donor visual theme;
- low-code workflow semantics;
- donor persistence model.

LCOS product truth remains the authority.

---

# 8. Files changed by A13 product patch

```text
ADD apps/web/src/features/spatial/projectMaterialRelationGesture.tsx
MOD apps/web/src/features/surfaces/SurfaceObject.tsx
MOD apps/web/src/features/surfaces/ContextSpaceSurface.tsx
MOD apps/web/src/features/surfaces/WorkflowSurface.tsx
MOD apps/web/src/features/surfaces/ProjectionSurfaces.tsx
MOD apps/web/src/features/canvas/ProjectCanvas.tsx
MOD apps/web/src/App.tsx
MOD apps/web/src/interaction-system.css
ADD scripts/validate-v015-a13-cross-surface-relation-gesture.mjs
MOD docs/v015/convergence/CONSTRUCTION_CONTEXT_INDEX_20260831.md
MOD docs/v015/convergence/FRONTEND_CONVERGENCE_PLAN_20260831.md
MOD docs/v015/convergence/GUI_RESPONSIBILITY_MATRIX_20260831.md
MOD docs/v015/convergence/GUI_PRODUCTION_OWNER_AUDIT_20260830.md
ADD docs/v015/convergence/A13_CROSS_SURFACE_RELATION_GESTURE_CLOSEOUT_20260831.md
```

No schema change. No Local Core contract change. No Phase B morphology change.

---

# 9. Source/static validation and acceptance

Actually executed in the formal construction tree:

```text
SOP-R1 validator: 8/8 PASS
A13 validator: 12/12 PASS
all runnable validate-v015-* gates: 41 PASS / 0 FAIL / 2 SKIP
  SKIP: S9 / S10 external semantic/provider gates
changed TSX syntax transpile: 7/7 PASS
git diff --check: PASS
```

Web semantic typecheck was attempted and remains:

```text
BLOCKED_ENV
TS2688: Cannot find type definition file for 'node'
TS2688: Cannot find type definition file for 'vite/client'
```

Acceptance:

- [x] Main keeps existing canonical persistence owner;
- [x] Context eligible Project materials get Orbit → Relation physical grammar;
- [x] Workflow eligible Project materials get the same physical grammar;
- [x] one shared transient adapter owns gesture only;
- [x] Context commit returns to canonical domain callback;
- [x] Workflow material commit returns to canonical domain callback;
- [x] Workflow Step→Step action linking remains separate;
- [x] generic Main `[data-node-id]` target fallback retired for ordinary Project-material Relation;
- [x] Conversation target semantics fail-close;
- [x] scope/workspace aggregate endpoints fail-close;
- [x] Relation intent suppresses competing transient controls;
- [x] A03–A12 regression validators remain green in the formal 41-gate sweep;
- [x] SOP-R1 removes the false permanent raw-source blocker without fabricating original text;
- [x] no Phase B/C expansion.

---

# 10. Runtime acceptance still required after local merge

Do not reinterpret source closeout as runtime proof.

On the user's real dependency environment, rerun:

```text
npm run typecheck --workspace @local-creative-os/web
relevant Vitest suites
node scripts/validate-v015-a13-cross-surface-relation-gesture.mjs
```

Human smoke:

```text
Main:    Select → Orbit → Relation → target → commit
Context: Select → Orbit → Relation → target → save/reload
Workflow material: Select → Orbit → Relation → target → save/reload
Workflow Step/action: existing step-link still works independently
Escape/cancel cleanup
no ordinary permanent relation notch
no target-click Orbit steal after commit
```

If local runtime evidence fails, stop and fix A13 before proceeding.

---

# 11. Remaining Phase A Relation debt

A13 does not close all Relation work.

Open:

1. Workspace source still uses explicit legacy notch owner;
2. Conversation Glyth Relation endpoint semantics remain unproven / fail-close;
3. aggregate `scope:*` / `workspace:*` canonical endpoint semantics remain unproven;
4. final receptor screen-space hit-slop / 12–18px edge-halo motor-tolerance acceptance remains open;
5. Context / Workflow real browser save→reload proof remains pending;
6. three-Surface Manual Product Smoke remains required before Phase B admission.

---

# 12. Formal status

```text
SOP-R1                         = PASS
A13 SOURCE / STATIC            = PASS
A13 MERGE AUTHORIZATION        = YES
POST-MERGE TYPECHECK           = PENDING LOCAL ENV
POST-MERGE UNIT                = PENDING LOCAL ENV
BROWSER E2E                    = PENDING
HUMAN PRODUCT SMOKE            = PENDING
PHASE A COMPLETE               = NO
PHASE B ADMISSION              = NO
```

---

# 13. STOP

A13 ends here. Do not automatically start the next Phase-A micro-patch.

After the user merges A13 and returns local runtime/manual evidence, run a fresh Phase-A parity census and select the next single blocking proposition.
