---

# Phase A Fresh Parity Census · after SOP-R1 + A13 local merge `6312ace`

Date: 2026-08-31
Baseline provenance: user real local merge validation reports `6312ace` over `59692ff` over `2d2f30c`; the supplied RC archive contains the merged source plus the 111-file context library.

## Census verdict

| Relation domain | Current owner truth | Classification after A13 | Next action |
|---|---|---|---|
| Main ordinary Project material | `ProjectObjectOrbit → Relation` + Main edge persistence | SOURCE/STATIC CLOSED | runtime/manual proof only |
| Context ordinary eligible Project material | shared `projectMaterialRelationGesture` + canonical Context domain Relation | SOURCE/STATIC CLOSED | runtime save→reload proof only |
| Workflow ordinary eligible Project material | shared physical adapter + canonical Workflow domain Relation | SOURCE/STATIC CLOSED | runtime save→reload proof only |
| Workflow Step→Step | `workflowActionState.edges` | CORRECT SEPARATE OWNER | keep separate |
| Workspace frame Relation source | permanent/hover legacy notch | **WRONG_OWNER** | **A14** |
| Conversation Glyth ordinary Relation | ordinary Relation semantics distinct from Conversation Context Mapping; canonical ordinary endpoint still not explicitly owned in GUI | SEMANTIC OWNER UNPROVEN | keep fail-close; do not guess |
| Conversation Context Mapping | Semantic Drop / Glyth Context Field → canonical Assembly `conversation_context` path | CORRECT SEPARATE OWNER | not A14 |
| Context/Workflow `scope:*` / `workspace:*` aggregate Relation | Domain supports `scope`/`workspace`, but A13 adapter intentionally accepts only view endpoints | SEMANTIC/PERSISTENCE ADAPTER DEBT | separate micro-patch after owner cleanup |
| Relation receptive edge motor tolerance | current body receptor / visual marker does not prove required extra 12–18px screen-space halo | ACCEPTANCE GAP | after owner cleanup |
| Browser/Human proof | local merge validation records E2E/manual BLOCKED_ENV | RUNTIME EVIDENCE GAP | required before Phase B |

## Why A14 is next

Phase A is still an ownership-cleanup phase. Workspace is the only remaining Relation item already proven to be a **wrong production owner** rather than a semantic question or final motor-tolerance acceptance.

Latest interaction truth remains:

```text
activate/select local spatial subject
→ local Orbit
→ Relation
→ Orbit yields
→ temporary source port
→ receptive target
→ commit
```

Workspace is not converted into an ordinary View or a second Selection truth. Its canonical aggregate endpoint remains `workspace:<workspaceId>` and existing Main relation persistence remains the owner.

## Conversation reclassification

Do not describe Conversation as merely “missing a Relation endpoint”. Surviving authority establishes:

```text
Ordinary Relation
!=
Conversation Context Mapping
```

Conversation Context Mapping is body/Semantic Drop onto Glyth and canonically persists through the Assembly / `conversation_context` path. Ordinary Glyth Relation may still exist as a separate semantic capability, but its canonical GUI endpoint must be proven before wiring. A14 therefore leaves it fail-close.

## A14 proposition

> Workspace Relation source ownership moves from a permanent/hover notch to a local transient Orbit intent while preserving the existing Workspace aggregate endpoint and Main relation persistence.

A14 does not touch:
- Conversation Relation semantics;
- Context/Workflow aggregate endpoint persistence;
- final 12–18px receptor halo;
- Object Species / Phase B.
