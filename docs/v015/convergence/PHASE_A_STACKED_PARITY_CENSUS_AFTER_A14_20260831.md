# Phase A Stacked Parity Census · after A14 source/static

Date: 2026-08-31

## Provenance boundary

Authoritative real local RC remains:

```text
6312ace
```

A14 is source/static closed and merge-authorized, but its real local merged HEAD / runtime smoke has not yet been returned in this conversation.

The user explicitly instructed `请继续` after A14 delivery. This authorizes one stacked source/static census and micro-patch; it does **not** manufacture A14 runtime evidence or a fake post-A14 HEAD.

Therefore this census evaluates:

```text
real RC 6312ace
+ A14 Workspace Relation Intent Ownership source patch
```

and carries all local runtime/manual debt forward.

---

## Remaining Relation items after A14 source ownership cleanup

| Item | Current truth | Classification | Decision |
|---|---|---|---|
| Main ordinary Project material | Orbit Relation + explicit receptor + Main persistence | source/static closed | keep |
| Context eligible Project material | shared A13 gesture + Context canonical Relation | source/static closed | keep |
| Workflow eligible Project material | shared A13 gesture + Workflow canonical Relation | source/static closed | keep |
| Workspace Relation launch | A14 local Orbit → Relation → temporary source port | source/static closed | keep; local runtime pending |
| Conversation ordinary Relation | ordinary Relation endpoint still not explicitly proven; distinct from Conversation Context Mapping | `SEMANTIC_OWNER_UNPROVEN` | fail-close |
| Context/Workflow `scope:*` / `workspace:*` aggregate Relation | view-only A13 adapter intentionally rejects aggregate IDs | `SEMANTIC/PERSISTENCE_ADAPTER_DEBT` | do not widen yet |
| Relation receptor tolerance | direct body hit only; visual target marker exists but physical 12–18px extra halo is not implemented | **L0 ACCEPTANCE GAP** | **A15** |
| Browser / Human proof | A14 has no returned real local merge proof yet | `RUNTIME_EVIDENCE_GAP` | carry forward |

---

## Why A15 is next

Conversation cannot be next because its ordinary Relation endpoint remains semantically unproven. Guessing `conversationViewId` or `conversationArtifactId` would violate fail-close and could conflate ordinary Relation with Conversation Context Mapping.

Aggregate scope/workspace support is an endpoint adapter capability expansion. It is not required to complete the already-frozen physical Relation grammar for ordinary materials.

By contrast, the latest Mandatory Context explicitly freezes:

```text
Relation receptive edge：额外 12–18px halo
```

Current source still resolves targets through `elementFromPoint`, so the pointer must physically land on the visible receptor body. A visual receptive marker does not satisfy the motor-tolerance acceptance.

Therefore the next smallest proven proposition is:

> A15 adds an invisible 16px **screen-space** receptor halo to the existing explicit Relation targets, while preserving visual bounds, Selection bounds, endpoint identity, and canonical persistence owners.

---

## A15 non-goals

A15 does not:

- add Conversation ordinary Relation;
- change Conversation Context Mapping;
- widen A13 eligibility to `scope:*` / `workspace:*` projections;
- change Workspace endpoint identity;
- change Main / Context / Workflow Relation persistence;
- add visual blur/glass halo;
- enter Object Species / Phase B.
