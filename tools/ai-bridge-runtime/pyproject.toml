[project]
name = "lcos-ai-bridge-runtime"
version = "0.2.0"
description = "Purified AI Bridge provider runtime baseline for Local Creative OS"
requires-python = ">=3.12,<3.14"
dependencies = [
  "mcp==1.28.1",
  "uvicorn>=0.35,<1",
]

[tool.unittest]
start-directory = "tests"
