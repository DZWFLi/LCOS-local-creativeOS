# Ownership and provenance

## Provenance

This baseline was extracted from the user-provided private local source:

```text
E:\Buddy项目\ai-bridge
```

Key imported source files were hash-compared with the migration research ZIP
dated 2026-07-28 and matched.

## Adoption mode

User-owned private source purification. This is not an adoption of Bridge
Runtime JSON, user configuration, Agent Skills or third-party application code.

No license file existed in the source package at review time. Therefore this
directory must be treated as private project source unless Dz supplies a
different licensing decision.

Python dependencies remain governed by their respective upstream licenses.

## LCOS ownership boundary

This provider runtime does not own LCOS Domain objects or lifecycle decisions.
LCOS-specific Canonical Run, RuntimeDispatch, ArtifactReturn, Revision, Current,
Accept, Reject and Retry semantics must be implemented in LCOS-owned TypeScript
code after a separate approval.
