# Recipe: Saved Context → Workflow

```text
selected materials
→ identify desired destination
→ infer minimal action stages
→ create lightweight Steps
→ attach original Entity inputs
→ connect sequence / branch
→ stop before execution
```

材料与步骤必须区分；同一材料不复制成新的 Workflow Truth。
