# CLI: presentation

```bash
lcos presentation list <project-id>
lcos presentation patch <project-id> <presentation-id> --json <patch.json>
```

reorganize 建议走 API proposal（ghost/apply/rollback），CLI patch 用于小改。
