# Recipe: Selection → Saved Context

```text
explicit Selection
→ remove duplicates by Entity identity
→ preserve Fragment source anchors
→ search only for clearly missing supporting evidence
→ propose membership / light grouping
→ review
```

不要为了“完整”自动扩大到整个 Project。
