# CLI: capture

```bash
lcos capture pending [--recent 30m]
lcos capture resolve <capture-id> --project <project-id>
lcos capture send --url <u> [--title 标题] [--session id] [--project id]
lcos project pin-capture <project-id>
lcos project unpin-capture
```

高置信 Affinity 自动进项目；不确定保持 staging，不弹强制选择器。
