# Policy: Provenance

- 关系必须带 origin（user/agent/system）+ createdBy。
- Agent 创建的关系可附 evidenceRefs（来源文件/对话引用）与 confidence；confidence 只是辅助，不是 Truth。
- 摘要节点注明来源：哪场对话、哪个文件、哪个 URL。
- 不把 Agent 推断写成用户原话；推断内容明确标注。
