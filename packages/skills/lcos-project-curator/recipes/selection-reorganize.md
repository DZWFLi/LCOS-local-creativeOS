# Recipe: Selection Reorganize

```text
读 Selection 视图
→ 找重复/孤岛/密度问题
→ 出 ReorganizeProposal（merge/remove/delete/hierarchy/emphasis）
→ ghost 预览 → 用户确认 → apply → rollback 入口提示
```

手工拖过（pinned）的节点默认不动。
