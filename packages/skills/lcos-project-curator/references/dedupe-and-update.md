# 去重与更新

## 流程

```text
1. lcos search 目标主题
2. 命中相似节点 → 读该节点（node read）
3. 判断：
   - 语义等价 → reuse（加关系/引用，不新建）
   - 同一主题持续沉淀 → update（追加/修正，不覆盖用户手写关键内容）
   - 新角度 → create
```

## 重复判据

```text
标题近义 + 内容表达同一结论 → 重复
标题近义但结论/范围不同 → 不是重复，考虑关系
```

## 安全

不确定时新建，不合并。
